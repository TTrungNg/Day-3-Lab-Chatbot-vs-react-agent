# Individual Report: Lab 3 - Chatbot vs ReAct Agent

- **Student Name**: Nguyễn Hữu Nam
- **Student ID**: 2A202600397
- **Date**: 6/4/2026

---

## I. Technical Contribution (15 Points)

*Describe your specific contribution to the codebase (e.g., implemented a specific tool, fixed the parser, etc.).*

- **Modules Implementated**: src/chatbot/chatbot.py (baseline chatbot Gemini version), src/agent/agent.py
- **Code Highlights**: None
- **Documentation**: The chatbot.py is implemented using Langchain and Gemini API and served as a baseline, while agent.py is my attempt at implementing a React agent using Langgraph, however these 2 have been discarded as a teammate of mine implemented a better version using OpenAI API.

---

## II. Debugging Case Study (10 Points)

*Analyze a specific failure event you encountered during the lab using the logging system.*

- **Problem Description**: The Langgraph agent knew which tool to used, passed a seemingly reasonable parameters to the tool but it doesnt work at all
- **Log Source**: None
- **Diagnosis**: The Langgraph agent kept inputting a dict in the format of {'query':str}, which is different from the expected input format of the tool implemented by my teammate.
- **Solution**: I wrapped the tool in a @tool wrapper from Langchain, changed the tool description to be more detailed, changed the input format into parsing a JSON generated by Gemini, however it took so much time so we went with the approach of another teammate.

---

## III. Personal Insights: Chatbot vs ReAct (10 Points)

*Reflect on the reasoning capability difference.*

1.  **Reasoning**: How did the `Thought` block help the agent compared to a direct Chatbot answer? The Thought block forces step-by-step planning, making the agent more structured and less likely to guess blindly.
2.  **Reliability**: In which cases did the Agent actually perform *worse* than the Chatbot? Probably when it misuses tools on simpler tasks, causing unnecessary complexity/ latency.
3.  **Observation**: How did the environment feedback (observations) influence the next steps? Observations provide feedback that helps the agent decide whether to keep using tools or to reply to the user.

---

## IV. Future Improvements (5 Points)

*How would you scale this for a production-level AI agent system?*

- **Scalability**: Implement caching and result reuse to reduce redundant computations and improve response time.
- **Safety**: Implement API key limit, add a LLM judge to ensure the safety of the answers.
- **Performance**: MongoDB, caching.

---

> [!NOTE]
> Submit this report by renaming it to `REPORT_[YOUR_NAME].md` and placing it in this folder.
